import Link from "next/link";
import { getDb } from "@/lib/db";

export default async function RecipientsPage() {
  const recipients = await getDb().recipient.findMany({ orderBy: { updatedAt: "desc" } });
  return <main className="min-h-screen bg-[#f6f8fb] px-5 py-8 sm:px-8 lg:px-12"><div className="mx-auto max-w-6xl"><header className="flex flex-col gap-5 border-b border-slate-200 pb-7 sm:flex-row sm:items-end sm:justify-between"><div><Link className="text-sm font-semibold text-slate-500 hover:text-indigo-700" href="/">← Dashboard</Link><p className="mt-7 text-xs font-semibold tracking-[0.14em] text-indigo-600 uppercase">Recipients</p><h1 className="mt-3 text-3xl font-semibold tracking-tight">People behind the outreach</h1><p className="mt-2 text-slate-600">Manage contact details and private recipient context.</p></div><Link className="rounded-xl bg-slate-950 px-4 py-3 text-center text-sm font-semibold text-white" href="/recipients/new">+ Add recipient</Link></header>{recipients.length === 0 ? <section className="mt-8 grid min-h-80 place-items-center rounded-2xl border border-dashed border-slate-300 bg-white p-8 text-center"><div><h2 className="text-xl font-semibold">Add your first recipient.</h2><p className="mt-2 text-sm text-slate-500">Contacts can be assigned to campaigns in the next step.</p><Link className="mt-6 inline-flex rounded-xl bg-slate-950 px-4 py-3 text-sm font-semibold text-white" href="/recipients/new">Add recipient</Link></div></section> : <section className="mt-8 overflow-hidden rounded-2xl border border-slate-200 bg-white">{recipients.map((recipient) => <Link className="flex items-center justify-between gap-4 border-b border-slate-100 px-5 py-5 last:border-0 hover:bg-slate-50" href={`/recipients/${recipient.id}`} key={recipient.id}><div><p className="font-semibold">{[recipient.firstName, recipient.lastName].filter(Boolean).join(" ") || "Unnamed recipient"}</p><p className="mt-1 text-sm text-slate-500">{recipient.email}</p></div><span className="text-sm text-indigo-700">Edit →</span></Link>)}</section>}</div></main>;
}
